rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Cada usuario solo puede leer, crear, actualizar o borrar su perfil.
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
